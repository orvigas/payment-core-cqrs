# Copy to .env (gitignored) and fill in real values before running
# docker-compose up. Never commit an actual secret here.

# HMAC-SHA256 JWT signing key, at least 32 bytes. Generate one with:
#   openssl rand -base64 48
JWT_SECRET=
